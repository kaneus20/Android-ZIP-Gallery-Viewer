package id.flwi.zipgalleryviewer.ui.screens.load

import androidx.lifecycle.ViewModel
import dagger.hilt.android.lifecycle.HiltViewModel
import javax.inject.Inject

/**
 * ViewModel for the Load screen.
 * Currently minimal as no complex state management is needed for the initial screen.
 */
@HiltViewModel
class LoadViewModel @Inject constructor() : ViewModel() {
    // Future functionality will be added here
}
